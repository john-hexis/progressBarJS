"use strict";
var button = (function() {
    var instance;

    function init() {
        function gen(id) {
            
        }

        return {

        }
    }

    return {
        ins: (function() {
            instance = init();
        })()
    };
})();